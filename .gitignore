node_modules

**/node_modules/
**/dist
**/build
# Turborepo daemon logs and local cache (build output, not source)
.turbo/
**/.turbo/

# Local Docker deployment secrets
.env.docker

# Local git worktree checkouts (working copies, not source)
.worktrees/
