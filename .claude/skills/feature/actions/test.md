# Test Compatibility Action

`test` is a compatibility alias for `/feature verify`.

Follow `verify.md` completely. Report that future workflows should use
`/feature verify` so functional verification, drift checks, builds, and tests
remain one consistent gate.
